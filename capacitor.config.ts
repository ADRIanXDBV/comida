import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'adrian_comida',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
